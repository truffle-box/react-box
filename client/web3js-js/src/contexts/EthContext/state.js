import Web3 from "web3";

const actionTypes = {
};

const initialState = {
};

const reducer = (state, action) => {
  switch (action.type) {
    default:
      throw new Error("Undefined reducer action type");
  }
};

export {
  actionTypes,
  initialState,
  reducer
};
